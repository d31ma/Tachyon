{
  "order": [
    "introduction",
    "routing",
    "devices",
    "templates",
    "native-rendering",
    "companions",
    "polyglot",
    "platform-apis",
    "storage",
    "yon",
    "operations",
    "cookbook"
  ],
  "groups": [
    {
      "title": "Start",
      "summary": "Install, project shape, routing and target choices.",
      "topics": [
        "introduction",
        "routing",
        "devices"
      ]
    },
    {
      "title": "Tac frontend",
      "summary": "Templates, polyglot companions and browser-local data.",
      "topics": [
        "templates",
        "native-rendering",
        "companions",
        "polyglot",
        "platform-apis",
        "storage"
      ]
    },
    {
      "title": "Yon backend",
      "summary": "Routes, middleware, validation and observability.",
      "topics": [
        "yon",
        "operations"
      ]
    },
    {
      "title": "Cookbook",
      "summary": "Production-shaped recipes that combine Tac and Yon.",
      "topics": [
        "cookbook"
      ]
    }
  ],
  "topics": {
    "introduction": {
      "title": "Introduction",
      "summary": "Tachyon is a polyglot, file-system-routed full-stack framework distributed through the standalone ty binary. Tac is the frontend layer and Yon is the backend/runtime layer.",
      "sections": [
        {
          "heading": "Install",
          "body": "Tachyon ships as the standalone `ty` binary — no package manager or runtime toolchain required. Scaffold an app and start the development server: Tachyon detects whether a project has a client/, a server/, or both, and serves the right thing on one port. Request validation is performed by CHEX, an external binary the server drives and names through TAC_CHEX_BINARY; identifiers are generated in-process.",
          "code": "curl -fsSL https://tachyon.del.ma/install.sh | sh\nty init my-app --name \"My App\"\ncd my-app\nty preview"
        },
        {
          "heading": "Layout",
          "body": "Pages live in client/pages, components in client/components, and backend routes in server/routes. A companion file sits beside its tac.html template; shared browser assets and data live under client/shared.",
          "code": "client/\n  pages/tac.html\n  components/hero/tac.html\n  components/hero/tac.swift\n  shared/assets/  shared/data/\nserver/\n  routes/posts/yon.js"
        }
      ]
    },
    "routing": {
      "title": "Routing",
      "summary": "Both layers route from the file system: a directory is a path, and dynamic segments use _slug directories that become :slug parameters at runtime.",
      "sections": [
        {
          "heading": "Frontend pages",
          "body": "client/pages/tac.html serves /, client/pages/docs/tac.html serves /docs, and client/pages/docs/_topic/tac.html serves /docs/:topic — the page you are reading uses exactly that dynamic segment. An ancestor page containing a slot element becomes a reusable shell for its descendants.",
          "code": "client/pages/\n  tac.html          -> /\n  docs/tac.html     -> /docs (shell with <slot />)\n  docs/_topic/\n    tac.html        -> /docs/:topic"
        },
        {
          "heading": "Backend routes",
          "body": "A route file at `server/routes/<path>/yon.<ext>` declares a class with `@Controller` whose static methods are HTTP verbs. The class name ends in `Controller`, and a controller declares HTTP methods and nothing else — the dispatch finds the method by name, so a helper beside GET would never be reached. A route may be split across sibling `yon` files declaring non-overlapping verbs, in any of the eight languages Yon runs.",
          "code": "@Controller\nexport class ItemsController {\n  static GET() { return { items: [] } }\n  static async POST(request) { return { created: request.body } }\n}"
        },
        {
          "heading": "Validation",
          "body": "An `OPTIONS.schema.json` beside each handler declares what a caller sends and receives, with CHEX regular-expression schemas: every value is a pattern and a `?` suffix marks an optional field. The server hands CHEX the declared schema and the request and relays its verdict, rather than re-implementing the schematics and creating a second interpretation of the same file.",
          "code": "{\n  \"methods\": {\n    \"POST\": {\n      \"request\": { \"body\": { \"name\": \"^.{1,80}$\", \"note?\": \"^.{0,400}$\" } },\n      \"ok\": { \"id\": \"^[0-9]+$\" }\n    }\n  }\n}"
        }
      ]
    },
    "yon": {
      "title": "Yon backend",
      "summary": "Yon owns server routes. A route is a directory holding a yon.<ext>; a handler is a class and its HTTP methods, in one of the eight languages that can declare a layer. One bounded process per request, with the protocol supplied rather than written.",
      "sections": [
        {
          "heading": "Serve modes",
          "body": "Tac and Yon are not separate servers and never were: one process builds the client and serves the routes. What a project has decides what it reports on start — \"Tac and Yon\", \"Tac only; no server/routes\", or \"Yon only; no client/pages\". `ty preview` rebuilds when sources change; `ty start` serves what was published and never rebuilds.",
          "code": "ty preview               # development\nty start --port 8080     # serve what was published"
        },
        {
          "heading": "Route convention",
          "body": "A route is a directory under `server/routes` holding a `yon.<ext>`. A directory beginning with an underscore is a dynamic segment, and it reaches the handler in `parameters` — the same way in every language Yon runs.",
          "code": "server/routes/items/yon.py          ->  /items\nserver/routes/items/_id/yon.py      ->  /items/:id\nserver/routes/api/search/_q/yon.js  ->  /api/search/:q"
        },
        {
          "heading": "Eight languages",
          "body": "Yon runs the languages whose syntax can carry a class annotation: JavaScript, TypeScript, Python, Java, C#, Kotlin, PHP and Rust. A handler declares its layer with `@Controller`, and a language that cannot make that declaration cannot be a handler — Go, Ruby, Perl, C and C++ are refused with TY2003 naming the supported set, and reached through `@Relay` instead. There is no shebang: Yon knows how to start each of the eight, so a handler declares its methods and nothing about its own execution.",
          "code": "yon.js, yon.ts    a built-in adapter, on a runtime that reads it\nyon.py            a built-in adapter\nyon.php           php, with the protocol runtime appended\nyon.java          JEP 330 single-file source launch\nyon.rs            rustc, with a proc-macro crate for the attributes\nyon.kt            kotlinc, run with java -jar\nyon.cs            a generated project, built with dotnet"
        },
        {
          "heading": "What a handler is",
          "body": "A class and its HTTP methods. Nothing reads standard input, nothing writes an envelope, and nothing declares a `main` — that half is Tachyon's, appended or compiled beside the file the author wrote. A method the handler does not declare answers 405 without being written. The class carrying `@Controller` is the handler, whatever it is called, and its name must end in `Controller` (TY2011).",
          "code": "@Controller\nclass ItemsController:\n    @staticmethod\n    def GET(request):\n        return {\"items\": []}\n\n    @staticmethod\n    def POST(request):\n        return {\"status\": 201, \"headers\": {}, \"body\": \"{}\"}"
        },
        {
          "heading": "Layers",
          "body": "A server class declares which layer it is in, and Tachyon checks the declaration against where the file sits (TY2008), against what it reaches (TY2009), against its own name (TY2011), and — for a controller — against what it declares (TY2012). A layer may reach a deeper one and never a shallower one, and the bottom three are peers rather than a stack. This is not dependency injection: a handler is one process per request, so nothing an injector built would outlive the response.",
          "code": "@Controller   server/routes         a request in, a response out\n@Service      server/services       owns a decision\n@Repository   server/repositories   owns storage\n@Client       server/clients        owns one outbound call\n@Delegate     server/delegates      owns work handed elsewhere"
        },
        {
          "heading": "Process lifecycle",
          "body": "One bounded process per request. No container, no singleton, no lifetime to reason about — the process starts, reads one request, writes one response and exits. A compiled handler is built once and cached under `.tachyon/handlers/<digest>`, so the build cost is paid on first use and every later request reuses the artefact; change the file and the digest changes with it. Deadlines, concurrency limits and frame bounds are enforced by the supervisor rather than by each language.",
          "code": ""
        },
        {
          "heading": "Route contracts",
          "body": "A route may publish an `OPTIONS.schema.json` describing what a caller sends and receives. Every value is a regular expression and a `?` suffix marks an optional field. Validation is performed by CHEX — the server hands it the declared schema and the request, and relays its verdict, rather than re-implementing the schematics and creating a second interpretation of the same file.",
          "code": "{\n  \"methods\": {\n    \"POST\": {\n      \"summary\": \"Create one item\",\n      \"request\": { \"body\": { \"name\": \"^.{1,80}$\", \"note?\": \"^.{0,400}$\" } },\n      \"ok\": { \"id\": \"^[0-9]+$\" },\n      \"clientError\": { \"error\": \"^.+$\" }\n    }\n  }\n}"
        },
        {
          "heading": "Streaming",
          "body": "A handler that yields is a stream: each value becomes one server-sent event, and the stream ends when the handler does. `@Stream` on the method is how the route says so before it is called — the server chooses the streaming path before the handler runs, so it cannot wait to see what comes back. The annotation and the body are checked against each other (TY2013), and `@Stream` in a language that cannot stream is refused (TY2014) rather than accepted and quietly ignored.",
          "code": "@Controller\nclass TicksController:\n    @staticmethod\n    @Stream\n    def GET(request):\n        for tick in range(1, 4):\n            yield {\"tick\": tick}"
        },
        {
          "heading": "Relaying to other languages",
          "body": "A program Yon does not run is still a program, and one that speaks Handler Protocol v1 on standard input and output is exactly what Yon spawns anyway. `@Relay` on a method makes it a proxy for one. The command sits on the declaration because it is metadata about the method, and it is explicit rather than inferred from the file name — a compiled Go binary and a Ruby script are the same call. A program that cannot be started answers 502 with the reason.",
          "code": "@Controller\nclass ReportController:\n    @staticmethod\n    @Relay(\"ruby\", \"server/delegates/report.rb\")\n    def GET(request): ..."
        },
        {
          "heading": "Middleware",
          "body": "A `middleware.<ext>` at the project root is consulted before every request and after every response. It answers `before` and `after` rather than an HTTP method, so it speaks the protocol itself: JavaScript, TypeScript, Python and PHP only. The compiled four are not supported, because the protocol runtime appended to a compiled handler dispatches on the method and would find neither.",
          "code": ""
        },
        {
          "heading": "Isolation",
          "body": "`YON_ISOLATION=process` is the default: one directly supervised child process per request. `YON_ISOLATION=firecracker` hands each request to a microVM control program instead, configured by `YON_FIRECRACKER_DRIVER`, `POOL`, `VCPUS`, `MEMORY_MIB` and `EGRESS`. It is read from the environment rather than a config file, because which isolation a deployment wants is a property of where it runs, not of the source — the same build runs either way. An invalid or half-declared policy fails as TY2010 before a handler runs.",
          "code": ""
        },
        {
          "heading": "Serving",
          "body": "`ty start` reads `YON_HOST` (or `YON_HOSTNAME`), `YON_PORT` and `YON_DIST_PATH`, and selects handler runtimes with `TACHYON_JAVASCRIPT_RUNTIME` and `TACHYON_PYTHON_RUNTIME`. The runtime choice matters for more than speed: a `yon.js` or `yon.ts` that declares `@Controller` needs a runtime that transpiles a decorator, and Node — the default — never will. Bun and Deno do.",
          "code": "YON_PORT=8080 \\\nYON_DIST_PATH=dist/web \\\nTACHYON_JAVASCRIPT_RUNTIME=$(command -v bun) \\\n  ty start"
        },
        {
          "heading": "JSON in every language",
          "body": "Java, Kotlin and Rust have no JSON in their standard libraries, and each compiles as a single file with nowhere to declare a dependency — so Tachyon supplies one, in scope without an import, under the same name in each: `Json.write` and `Json.parse`. C# has one in the box and gets the same name wrapped around it. PHP, JavaScript, TypeScript and Python use their own.",
          "code": "// Rust\nYonResponse::json(Json::map([(\"products\", Json::list([\"anvil\"]))]))\n\n// Java\nYonResponse.json(Json.write(java.util.Map.of(\"products\", java.util.List.of(\"anvil\"))));\n\n// C#\nYonResponse.Json(Json.Write(new { products = new[] { \"anvil\" } }));"
        }
      ]
    },
    "templates": {
      "title": "Tac templates",
      "summary": "Templates are HTML with braces for interpolation, on:event attributes for delegated events, loop, logic, and switch elements for control flow, and explicit island hydration policies.",
      "sections": [
        {
          "heading": "Interpolation and events",
          "body": "Braces evaluate JavaScript inside the async render function, so await is available anywhere. Event handlers are single expressions compiled to one delegated listener per event type, registered at the document and hydrated off the critical path with lossless replay.",
          "code": "<h1>{headline}</h1>\n<button on:click=\"refresh()\">Refresh</button>\n<input :value=\"name\" on:input=\"name = $event.target.value\" />"
        },
        {
          "heading": "Control flow",
          "body": "Loops, conditionals and switches are elements, so structure stays visible in the markup.",
          "code": "<loop :for=\"post of posts\">\n  <article>{post.title}</article>\n</loop>\n<logic :if=\"ready\"><p>Ready.</p></logic>\n<switch :value=\"status\">\n  <case :when=\"'loading'\"><p>Working…</p></case>\n  <case default><p>Done.</p></case>\n</switch>"
        },
        {
          "heading": "Components",
          "body": "A component folder under client/components maps to a hyphenated tag: components/panel/users becomes panel-users. Props pass as attributes, and adding the lazy attribute defers loading until the component scrolls into view."
        },
        {
          "heading": "Hydration schedules",
          "body": "Tac has no server-side rendering mode: the browser renderer owns initial DOM creation and every rerender, and the document a route serves contains no rendered view subtree. So `hydrate` does not say when server HTML is adopted — it says when a component's companion activates.\n\n`load` activates immediately, `idle` waits for the browser to be free, `visible` waits for the viewport, `interaction` waits for the first event, and `never` leaves the component inert. A component that only displays what it was given needs no companion at all.",
          "code": "<product-card hydrate=\"visible\" :sku=\"sku\" />"
        },
        {
          "heading": "Hydration is a literal",
          "body": "`hydrate` must be one of `load`, `idle`, `visible`, `interaction` or `never`, written as a static literal. It cannot be an expression: the schedule is compiled into the render plan, and a value that is only known at run time would have to be resolved after the moment it was meant to decide.\n\nProperties are ordinary values passed to the component. They are evaluated in the browser like everything else, so nothing about them is written into the served document.",
          "code": ""
        }
      ]
    },
    "native-rendering": {
      "title": "Native hosts",
      "summary": "A native target embeds the platform's own WebView and loads the application's own bundle, so a desktop or mobile app renders exactly what the browser renders. There is one rendering of an application, not two.",
      "sections": [
        {
          "heading": "One rendering",
          "body": "`web` is a browser target. macOS, Windows, Linux, iOS and Android each embed the WebView their platform already provides — WKWebView, WebView2, WebKitGTK — and load the built bundle's `index.html` from disk. What a browser shows is what the application shows, because it is the same bundle.\n\nMatching a platform's look is a choice made in the UI layer, with whichever kit the application already uses. It is not something the framework decides by mapping tags to controls.",
          "code": ""
        },
        {
          "heading": "One HTML source",
          "body": "An application writes `tac.html` once. Control elements — `logic`, `loop`, `switch`, `case` — and Tac components compile away before publication, and what ships is the document a browser would have rendered. No second tree, no per-platform view language, and no tag that is supported on one target and not another.",
          "code": ""
        },
        {
          "heading": "The engine is the platform's",
          "body": "Tachyon builds and vendors no JavaScript engine. Each host uses the one its platform already ships, which is the same engine backing the WebView it embeds.",
          "code": "macOS, iOS   JavaScriptCore   a system framework; nothing to install\nLinux        JavaScriptCore   ships with the WebKitGTK the host links\nWindows      WebView2         Microsoft's current supported embedding API"
        },
        {
          "heading": "What the host adds",
          "body": "Three things. `data-platform=\"native\"` on the document body, so a page can behave differently without sniffing a user agent. `tachyonWindow`, carrying the window controls the manifest granted and nothing else. And the companion channel, in both directions.\n\nThere is no capability vocabulary beyond that, deliberately: a compiled companion reaches its platform's SDK because it was compiled as that language for that target, so a list of verbs in the middle would only ever be the subset Tachyon had implemented.",
          "code": ""
        },
        {
          "heading": "Companions are compiled in",
          "body": "A `tac.swift`, `tac.kt`, `tac.cs` or `tac.rs` beside a view is compiled for the target and linked into the host process — it is not interpreted, and not shipped as source. That is why it reaches AppKit or the Android framework directly, and why a route needs a companion that runs on the target being built (TY1010) rather than one that is merely present.",
          "code": ""
        },
        {
          "heading": "What the host does not do",
          "body": "There is no managed remote-content surface, no always-on-top or opacity control beyond what the manifest grants, no global shortcut registry and no system tray API. Those were a fixed verb vocabulary compiled into each host, and a fixed vocabulary is a ceiling. An application that needs one of them reaches for it from its companion, in the language of the platform it is running on.",
          "code": ""
        }
      ]
    },
    "companions": {
      "title": "Companions",
      "summary": "A tac.js or tac.ts file beside a template is instantiated automatically; its fields and methods are visible to the template without wiring.",
      "sections": [
        {
          "heading": "Reactive fields",
          "body": "Assigning to a declared field schedules one batched rerender of the component subtree. Fields prefixed with $ persist to sessionStorage; $$ fields persist to localStorage. Props auto-bind onto same-named declared fields.",
          "code": "export default class {\n  count = 0\n  $visits = 0\n  $$theme = 'light'\n\n  increment() { this.count += 1 }\n}"
        },
        {
          "heading": "Decorators",
          "body": "Stage 3 decorators express the same things declaratively, and the compiler brings them into scope — `@publish` and `@subscribe` for signals between components, and `@onMount` for lifecycle. A `@subscribe` may ask to run on mount as well as on each signal.",
          "code": "export default class {\n  @subscribe release\n  @publish('release') appVersion = 'TACHYON'\n\n  @subscribe('demo-refresh', { onMount: true })\n  refresh() {}\n\n  @onMount\n  initialize() {}\n}"
        }
      ]
    },
    "polyglot": {
      "title": "Polyglot companions",
      "summary": "Tac companions are colocated controllers for templates. They are portable across web, desktop and mobile outputs and do not require a client worker endpoint.",
      "sections": [
        {
          "heading": "Choose a companion language",
          "body": "A component or page may use `tac.js`, `tac.ts`, `tac.rs`, `tac.kt`, `tac.swift` or `tac.cs` beside its `tac.html`. JavaScript and TypeScript reach every target; the rest reach the targets they are the language of — Rust on desktop, Swift on Apple, Kotlin on Android, C# on Windows. A route with no companion that runs on the target being built is refused with TY1010 naming what it declared. Dart is not a companion language: a companion is compiled for the target it runs on, and Dart has no target of its own.",
          "code": "client/components/counter/\n  tac.html\n  tac.swift\n\nfinal class Counter: Tac {\n  var count: Int = 0\n  func increment() { self.count += 1 }\n}"
        },
        {
          "heading": "What a companion reaches",
          "body": "There is no capability vocabulary in the middle. A compiled companion is linked into the host process, so the platform's own SDK is simply there — AppKit, UIKit, the Android framework, the file system — reached because the companion was compiled as that language for that target. A fixed list of verbs in the middle could only ever be the subset Tachyon had got round to implementing, in three hosts, for every platform at once.\n\nWhat the prelude does supply is the two lifetimes the browser gives a page, spelled the way each language spells a property that persists itself, and one channel back to the page.",
          "code": "// Swift — @Stored writes through to UserDefaults, @Session lasts the process\n@Stored(\"theme\") var theme: String = \"calm\"\ntacPublish(\"probe.tick\", 1)\n\n// Kotlin\nTac.session(\"theme\", \"calm\")\ntacPublish(\"probe.tick\", 1)\n\n// Rust\ntac_session(\"theme\", \"calm\");\ntac_publish(\"probe.tick\", 1);"
        },
        {
          "heading": "Contracts stay on the server",
          "body": "OPTIONS.schema.json remains the Yon route contract for incoming requests and outgoing responses. Tac companions are UI controllers, so they call normal HTTP endpoints through the local-first fetch binding rather than declaring a second route protocol."
        },
        {
          "heading": "Where the boundary is",
          "body": "The boundary is the language, not a capability list. A `tac.js` runs everywhere and reaches what a browser reaches. A `tac.swift` runs on Apple targets and reaches what Swift reaches there. Nothing in between decides what is permitted, so nothing in between has to be extended before an application can use something its platform already offers.\n\nWhat is checked at build time is reach: a route whose only companion is a `tac.swift` cannot be built for Android, and TY1010 says so, naming what the route declared.",
          "code": "error[TY1010]: Route '/' has no companion that runs on android.\n               It declares Swift (macos, ios)."
        }
      ]
    },
    "devices": {
      "title": "Devices and targets",
      "summary": "Tac builds one client source tree into a browser output or native desktop and mobile controls. The rooted terminology: platform is the form factor (desktop/mobile/web); environment and os are synonyms for the concrete host (windows/macos/linux/android/ios/web); target is the bundle output.",
      "sections": [
        {
          "heading": "Targets",
          "body": "The supported bundle targets are web, macos, windows, linux, ios and android. web emits browser assets; every non-web target uses the native renderer by default. The all target expands to every supported target and writes sibling directories under dist/.",
          "code": "ty bundle --target web      # dist/web\nty bundle --target macos    # dist/macos\nty bundle --target windows  # dist/windows\nty bundle --target linux    # dist/linux\nty bundle --target ios      # dist/ios\nty bundle --target android  # dist/android\nty bundle --target all"
        },
        {
          "heading": "One bundle, every target",
          "body": "Every non-web target embeds the platform's own WebView and loads the application's own bundle. There is no tag mapping and no fallback boundary to reason about, because there is nothing to fall back from — the bundle renders identically everywhere.\n\nTachyon used to lower a Tac view into SwiftUI, Compose and WinUI controls and drop into a web view wherever no adapter existed. On a real design almost everything fell back, and what did not looked nothing like the rest. The `--render-mode` flag and `TAC_RENDER_MODE` went with it and are refused if passed.",
          "code": "ty bundle --target macos\nty bundle --target android\nty bundle --target windows"
        },
        {
          "heading": "Preview",
          "body": "Preview accepts exactly one target. If you omit --target, Tachyon previews web. Native previews run prerequisite checks first so missing Xcode or Swift, the Android SDK, Windows build tools, GTK, embedded controller dependencies, or a WebView dependency required by an inferred boundary fail early with an actionable message.",
          "code": "ty preview --target web\nty preview --target macos\nty preview --target android"
        },
        {
          "heading": "Runtime globals",
          "body": "Templates can read platform, environment, os and target without imports. Every companion language can read the same structured context through its Tac runtime binding.",
          "code": "<switch :value=\"platform\">\n  <case :when=\"'desktop'\">Desktop bundle on {os}</case>\n  <case :when=\"'mobile'\">Mobile bundle on {os}</case>\n  <case default>Web platform</case>\n</switch>\n\nconst platform = this.tac.platform.platform  // desktop | mobile | web\nconst os = this.tac.platform.os              // windows | macos | linux | android | ios | web"
        },
        {
          "heading": "Rooted terminology",
          "body": "platform is the form factor: macos, windows and linux targets are the desktop platform; ios and android are the mobile platform; web is the web platform. environment and os are synonyms for the concrete host: the operating system for native bundles, web for browser bundles. target names the bundle output. Companion language names the language a Tac companion is written in: JavaScript, TypeScript, Rust, Kotlin, Swift or C#."
        }
      ]
    },
    "storage": {
      "title": "Storage and data",
      "summary": "What a browser keeps: fields that outlive a render, responses that outlive a request, and assets that outlive the network. What a server pushes: topics, over server-sent events.",
      "sections": [
        {
          "heading": "Persisted fields",
          "body": "A field prefixed `$` persists for the tab and a field prefixed `$$` persists across sessions — sessionStorage and localStorage, named for how long they last rather than for the API behind them. Assigning to either schedules one batched rerender, the same as any other declared field. Reading storage can throw outright in a sandboxed frame, so even acquiring it is guarded and a field simply falls back to its declared value.",
          "code": "export default class {\n  $draft = ''      // this tab\n  $$theme = 'calm' // every visit\n}"
        },
        {
          "heading": "Cached reads",
          "body": "`tac.fetch` caches a response so a repeated read does not repeat the request. A static asset is precached so a route the visitor has already seen opens without the network. Neither is a database: Tachyon ships no client-side collection, no `find`, and nothing that mirrors a server table into the browser. A handler is where data lives, and a route is how a page reaches it.",
          "code": ""
        },
        {
          "heading": "Topics",
          "body": "A topic is an append-only log under `.tachyon/topics`, served as server-sent events from `/.tachyon/topics/<name>`. A subscriber holds the stream open and resumes from its last position, so a browser that was away replays what it missed rather than losing it. A log is read up to 16 MiB in one pass and polled for new records.\n\nA route that streams its own values is a different thing — that is `@Stream` on a handler, under Yon.",
          "code": "const stream = new EventSource('/.tachyon/topics/inventory')\nstream.addEventListener('message', (event) => {\n  console.log(JSON.parse(event.data))\n})"
        },
        {
          "heading": "Signals between components",
          "body": "A document-scoped publish and subscribe bus carries values between components that have no parent in common. `@publish` on a field sends its value under a name, `@subscribe` receives it, and the bus retains the last value so a component that mounts later is not left waiting for the next one. One failing subscriber does not stop the others.",
          "code": "export default class {\n  @publish('release') appVersion = '26.31.06'\n\n  @subscribe release\n}"
        }
      ]
    },
    "cookbook": {
      "title": "Cookbook",
      "summary": "Concrete recipes for building Tachyon apps without leaving the frontend-first workflow. This website is frontend-only, so these patterns can be explored with Tac companions before a Yon server exists.",
      "sections": [
        {
          "heading": "Frontend-only prototype",
          "body": "Start with client/ only. Tac renders pages and components, and companions provide the reactive UI controller. When you later add server/routes, the UI contract can stay the same.",
          "code": "client/\n  pages/tac.html\n  components/panel/users/tac.html\n  components/panel/users/tac.kt\n  shared/data/docs.json\ndb/\n  schemas/users/manifest.json"
        },
        {
          "heading": "Signal-driven UI",
          "body": "Publish and subscribe decorators replace manual event wiring between companion scripts. A form can publish a collection change and every interested panel can refresh itself without knowing who sent the signal.",
          "code": "export default class {\n  @publish('users:changed') changed = 0\n\n  async save() {\n    await this.tac.fetch('/users', { method: 'POST', body: this.form })\n    this.changed += 1\n  }\n}\n\nexport default class {\n  @subscribe('users:changed', { onMount: true })\n  async refresh() {}\n}"
        },
        {
          "heading": "Polyglot middleware boundary",
          "body": "A `middleware.<ext>` at the project root is consulted before every request and after every response. It answers `before` and `after` rather than an HTTP method, so it speaks the protocol itself — JavaScript, TypeScript, Python and PHP. The compiled languages are not supported for middleware, because the protocol runtime appended to a compiled handler dispatches on the HTTP method and would find neither phase.",
          "code": "middleware.py\nserver/\n  routes/users/yon.ts\n\nclass Middleware:\n    @staticmethod\n    def before(input):\n        return { \"action\": \"continue\" }"
        },
        {
          "heading": "Native target polish",
          "body": "Design once against the environment and platform globals, then bundle to web, desktop and mobile outputs. The same Tac template can choose browser, desktop or mobile copy without creating separate pages.",
          "code": "<switch :value=\"environment\">\n  <case :when=\"'desktop'\">Desktop workspace for {platform}</case>\n  <case :when=\"'mobile'\">Pocket workspace for {platform}</case>\n  <case default>Browser workspace</case>\n</switch>"
        }
      ]
    },
    "operations": {
      "title": "Operations",
      "summary": "Health and readiness endpoints, generated OpenAPI docs, middleware hooks, rate limiting, and OpenTelemetry spans come built in.",
      "sections": [
        {
          "heading": "Observability",
          "body": "Every request carries a correlation identifier, generated in-process and threaded through the handler protocol, so a request and the handler invocation it caused can be tied together in a log. Failures arrive as Diagnostics v1 with stable codes — the same structure a build emits — and `--diagnostic-format json` makes them machine-readable. Tracing exporters are not built in.",
          "code": "ty preview --diagnostic-format json\nty doctor            # toolchains and handler runtimes"
        },
        {
          "heading": "Build and deploy",
          "body": "ty bundle writes dist/web by default. Use --target all during release validation, then preview or package the individual target you intend to ship.",
          "code": "ty bundle --target all\nty preview --target web\nty preview --target macos"
        },
        {
          "heading": "Security",
          "body": "Every response carries defensive headers, and a strict content security policy is one of them — `script-src 'self'`, no inline script, `object-src 'none'`, `frame-ancestors 'none'`. A handler runs as one bounded process with a deadline and a concurrency limit, its frames are bounded at 16 MiB, and a declared route contract is validated by CHEX before the handler is called at all.\n\nThere is no built-in authentication and no CORS layer. Both belong to the application: a middleware answers `before` every request, which is where a policy of either kind goes.",
          "code": "cache-control: no-store\nreferrer-policy: no-referrer\nx-frame-options: DENY\nx-content-type-options: nosniff\ncontent-security-policy: default-src 'self'; style-src 'self' 'unsafe-inline';\n  img-src 'self' data:; script-src 'self' 'wasm-unsafe-eval'\n  'inline-speculation-rules'; object-src 'none'; base-uri 'none';\n  frame-ancestors 'none'"
        }
      ]
    },
    "platform-apis": {
      "title": "Platform APIs",
      "summary": "A page in a browser is a page: it uses the web platform directly. A page in a native host is the same page, in the platform's own WebView, with a small shim around it — and a compiled companion beside it that reaches the platform's SDK because it was compiled as that language for that target.",
      "sections": [
        {
          "heading": "There is no capability vocabulary",
          "body": "Tachyon does not put a list of verbs between an application and its platform. A tray, a notification, a file dialog — those are the platform's, and a companion written in the platform's own language already has them. A fixed list in the middle could only ever be the subset Tachyon had got round to implementing, in three hosts, for every platform at once, and an application would be stuck behind whatever that subset happened to be.\n\nSo the browser half uses web APIs, and the native half uses AppKit, UIKit, the Android framework or Win32 — each from the language that already speaks it.",
          "code": ""
        },
        {
          "heading": "What the host injects",
          "body": "Three things, and deliberately no more. The document body carries `data-platform=\"native\"`, so a page can style or behave differently without sniffing a user agent. `tachyonWindow` carries the window controls the manifest granted. And the companion channel runs in both directions — a companion publishes to the page, and the page invokes the companion.\n\nThe publish stub queues rather than delivers: a companion may publish from a thread of its own before the page's modules have run, and without the queue that value is lost to a race nothing in the application can see.",
          "code": "if (document.body.dataset.platform === 'native') {\n  // running inside a host\n}\n\nglobalThis.tachyonWindow?.minimize()"
        },
        {
          "heading": "Window controls are granted, not assumed",
          "body": "`manifest.json` — the file a PWA already ships — declares the window a native target opens and what a page may do to it, under a `tachyon` member a browser ignores. Nothing is granted by default. A control that was not granted is not generated into the host at all, so there is no code to reach rather than a check that refuses, and a page can ask what it has by testing for the function.\n\nElectron hands a privileged process the whole window. Tauri gates each call behind a permission in a capabilities dialect of its own. This is the same default-deny, in the file the application already has.",
          "code": "{\n  \"tachyon\": {\n    \"window\": {\n      \"width\": 1024,\n      \"height\": 768,\n      \"controls\": [\"minimize\", \"maximize\", \"close\"]\n    }\n  }\n}"
        },
        {
          "heading": "Portable companion spelling",
          "body": "A companion is written in its own language and reaches its own platform directly, so there is no portable verb list to learn. What is shared is the shape: a class whose fields are state and whose methods are behaviour, the two persistence lifetimes the browser gives a page, and `tacPublish` to send a value back. Each language spells those the way it spells everything else.",
          "code": "// Swift — @Stored writes through to UserDefaults\n@Stored(\"theme\") var theme: String = \"calm\"\n\n// Kotlin\nTac.setSession(\"theme\", \"calm\")\n\n// Rust\ntac_set_session(\"theme\", \"calm\");"
        },
        {
          "heading": "What the host records",
          "body": "A packaged host writes `tachyon.host.json` beside its bundle, naming the target it was built for and the one capability the bridge offers: `companion.invoke`. It is one line because there is one thing to say — everything else an application reaches, it reaches through its own language.",
          "code": "{ \"hostCapabilities\": [\"companion.invoke\"] }"
        }
      ]
    }
  }
}
