{
  "expected_divergences": [
    {
      "check": "semantic dom /",
      "detail": "<main>[1]: <product-card> vs <tachyon-component>",
      "reason": "Both implementations retain a semantically neutral runtime component boundary, but the legacy implementation uses the authored custom-element name while the client-only Rust renderer uses the framework-owned tachyon-component boundary. Slotted content and accessible semantics are identical.",
      "direction": "intentional"
    }
  ]
}
