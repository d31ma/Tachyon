#!/usr/bin/env dart
import 'dart:convert'; 
import 'dart:io'; 

void main() { 

	print('Executing Dart...');

  var request = jsonDecode(stdin.readLineSync()!);

  print(request["context"]);

  stdout.write(jsonEncode({ "message": "Hello from Dart!"}));
}