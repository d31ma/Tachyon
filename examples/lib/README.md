# lib/

Place polyglot runtime dependencies here (e.g. JARs, shared libraries) that route handlers need at execution time.

These files are **not** managed by a package manager — add or remove them manually.
